#include <iostream>

using namespace std;

int main(){
    // quoted constant: single quote for character, double quote for strings
    // other type quoted constant of constant is wchar_t, char16_t, char32_t (for unicode, supporting multiple language)

    // C++ 11 feature
    auto x = 5U; // unsigned integer

    // there are also literal for double, long, etc.

    return 0;
}