#include <iostream>

using namespace std;

int main(void){
    // when creating a variable, we need to specify the type of data that needed to be stored
    // we can see variable as a container to store value(s)
    
    // e.g.: a string variable
    string name = "tim";
    int id = 7;

    cout << "Hello " << name << ", your id is " << id << endl;

    return 0;
}